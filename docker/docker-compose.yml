version: "3.9"

services:
  openvassal:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    volumes:
      # Mount personal data directory for persistence
      - ./data:/app/data
      # Mount .env for API keys
      - ../.env:/app/.env:ro
    environment:
      - DATABASE_PATH=/app/data/openvassal.db
    stdin_open: true
    tty: true
